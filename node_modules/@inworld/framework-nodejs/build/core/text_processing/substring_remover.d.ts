import { VectorString } from '../../common';
import { ExternalReference } from '../../common/types';
import { StreamTextProcessor } from './stream_text_processor';
/**
 * Text processor that removes specified substrings from text streams.
 * Extends the StreamTextProcessor with substring removal functionality.
 */
export declare class SubstringRemover extends StreamTextProcessor {
    /**
     * Creates a new SubstringRemover instance asynchronously.
     *
     * @param textStream - External reference to the text stream
     * @param substrings - Vector of substrings to remove from the text
     * @returns Promise resolving to a new SubstringRemover instance
     * @throws InworldError if creation fails
     */
    static create(textStream: ExternalReference, substrings: VectorString): Promise<any>;
}
