import { ExternalReference } from '../../common/types';
import { StreamTextProcessor } from './stream_text_processor';
/**
 * Text processor that removes emoji characters from text streams.
 * Extends the StreamTextProcessor with emoji removal functionality.
 */
export declare class EmojiRemover extends StreamTextProcessor {
    /**
     * Creates a new EmojiRemover instance asynchronously.
     *
     * @param textStream - External reference to the text stream
     * @returns Promise resolving to a new EmojiRemover instance
     * @throws InworldError if creation fails
     */
    static create(textStream: ExternalReference): Promise<any>;
}
