import { TextStreamIterator, TTSOutputStreamIterator } from '../../common/stream';
/**
 * Represents a function that returns a promise of a text response.
 */
export type TextResponse = {
    text?: string;
    done: boolean;
};
/**
 * Represents a function that returns a promise of an audio response.
 */
export type AudioResponse = {
    text?: string;
    audio?: {
        data: number[];
        sampleRate: number;
    };
    done: boolean;
};
/**
 * Represents the type of a response from a graph output stream.
 */
export declare enum GraphOutputStreamResponseType {
    TEXT = "TEXT",
    KNOWLEDGE = "KNOWLEDGE",
    TEXT_STREAM = "TEXT_STREAM",
    TTS_OUTPUT_STREAM = "TTS_OUTPUT_STREAM"
}
/**
 * Represents a response from a graph output stream.
 */
export type GraphOutputStreamResponse = {
    /**
     * The data returned from the graph output stream.
     */
    data?: TextStreamIterator | TTSOutputStreamIterator | string[] | string;
    /**
     * The type of the graph output stream.
     */
    type?: GraphOutputStreamResponseType;
    /**
     * Whether the graph has finished executing.
     */
    done: boolean;
};
