import { Node, NodePropsInterface } from '../node';
/**
 * Interface for Text Chunking Node creation properties.
 * No additional properties beyond the base NodePropsInterface.
 */
export interface TextChunkingNodePropsInterface extends NodePropsInterface {
}
/**
 * Node for breaking text into smaller chunks or segments.
 * Useful for processing long text into manageable pieces.
 */
export declare class TextChunkingNode extends Node {
    /**
     * Creates a new TextChunkingNode instance.
     *
     * @param props - Text chunking node creation properties
     * @internal
     */
    constructor(props: TextChunkingNodePropsInterface);
    /**
     * Cleans up resources associated with this text chunking node.
     */
    destroy(): void;
}
