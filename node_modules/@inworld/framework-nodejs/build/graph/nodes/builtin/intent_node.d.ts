import { IntentMatcher } from '../../../primitives/intent/intent_matcher';
import { Node, NodePropsInterface } from '../node';
import { IntentNodeExecutionConfigPropsInterface } from './node_execution_config';
/**
 * Interface for Intent Node creation properties.
 */
export interface IntentPropsNodeInterface extends NodePropsInterface {
    /** Intent matcher instance for processing text */
    intentMatcher: IntentMatcher;
    /** Configuration for the execution */
    executionConfig?: IntentNodeExecutionConfigPropsInterface;
}
/**
 * Node for intent recognition and classification in the graph.
 * Processes text input through an intent matcher to identify user intents.
 */
export declare class IntentNode extends Node {
    private intentMatcher;
    private executionConfig;
    /**
     * Creates a new IntentNode instance.
     *
     * @param props - Intent node creation properties
     * @internal
     */
    constructor(props: IntentPropsNodeInterface);
    /**
     * Cleans up resources associated with this intent node.
     */
    destroy(): void;
}
