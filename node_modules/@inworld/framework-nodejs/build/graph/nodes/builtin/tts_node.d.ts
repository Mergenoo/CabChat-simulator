import { SpeechSynthesisConfigInterface, TTS } from '../../../primitives/tts';
import { Node, NodePropsInterface } from '../node';
import { TTSNodeExecutionConfigPropsInterface } from './node_execution_config';
/**
 * Interface for TTS Node creation properties.
 */
export interface TTSNodePropsInterface extends NodePropsInterface {
    /** TTS (Text-to-Speech) instance for speech synthesis */
    tts: TTS;
    /** Configuration for speech synthesis parameters */
    synthesisConfig: SpeechSynthesisConfigInterface;
    /** Execution configuration for the TTS node */
    executionConfig?: TTSNodeExecutionConfigPropsInterface;
}
/**
 * Node for text-to-speech conversion in the graph.
 * Processes text input to generate speech audio.
 */
export declare class TTSNode extends Node {
    private tts;
    private executionConfig;
    /**
     * Creates a new TTSNode instance.
     *
     * @param props - TTS node creation properties
     * @throws Error if creation fails
     * @internal
     */
    constructor(props: TTSNodePropsInterface);
    /**
     * Cleans up resources associated with this TTS node.
     */
    destroy(): void;
}
