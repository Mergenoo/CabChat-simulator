"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TTSNode = void 0;
const expose_binary_1 = require("../../../expose_binary");
const node_1 = require("../node");
const node_execution_config_1 = require("./node_execution_config");
/**
 * Node for text-to-speech conversion in the graph.
 * Processes text input to generate speech audio.
 */
class TTSNode extends node_1.Node {
    /**
     * Creates a new TTSNode instance.
     *
     * @param props - TTS node creation properties
     * @throws Error if creation fails
     * @internal
     */
    constructor(props) {
        let executionConfig;
        try {
            executionConfig = new node_execution_config_1.TTSNodeExecutionConfig(props.executionConfig);
            super(props.id, expose_binary_1.NodeHelperFunctions.createTTSNode(props.id, props.tts.getExternal(), executionConfig.getExternal()));
            this.tts = props.tts;
            this.executionConfig = executionConfig;
        }
        catch (e) {
            executionConfig === null || executionConfig === void 0 ? void 0 : executionConfig.destroy();
            throw e;
        }
    }
    /**
     * Cleans up resources associated with this TTS node.
     */
    destroy() {
        var _a, _b;
        (_a = this.tts) === null || _a === void 0 ? void 0 : _a.destroy();
        this.tts = null;
        (_b = this.executionConfig) === null || _b === void 0 ? void 0 : _b.destroy();
        this.executionConfig = null;
        if (this.external) {
            expose_binary_1.NodeHelperFunctions.deleteTTSNode(this.external);
            super.destroy();
        }
    }
}
exports.TTSNode = TTSNode;
