"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChatMessages = void 0;
const common_1 = require("../../common");
const expose_binary_1 = require("../../expose_binary");
const base_data_1 = require("./base_data");
/**
 * Class for handling chat messages in graphs.
 * Extends BaseData with chat message-specific functionality.
 * @internal
 */
class ChatMessages extends base_data_1.BaseData {
    /**
     * Creates a new ChatMessages instance.
     *
     * @param messages - Array of chat messages
     */
    constructor(messages) {
        let mapString;
        try {
            mapString = new common_1.MapString(messages);
            super(expose_binary_1.ChatMessagesFunctions.new(mapString.getExternal()));
        }
        catch (e) {
            mapString === null || mapString === void 0 ? void 0 : mapString.destroy();
            throw e;
        }
    }
    /**
     * Cleans up resources used by the ChatMessages instance.
     */
    destroy() {
        var _a;
        (_a = this.mapString) === null || _a === void 0 ? void 0 : _a.destroy();
        this.mapString = null;
        super.destroy();
    }
}
exports.ChatMessages = ChatMessages;
