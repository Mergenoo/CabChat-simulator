import { ExternalReference } from '../../common/types';
/**
 * Class representing a matched intent.
 * Contains information about an intent that was detected in user input.
 */
export declare class IntentMatch {
    private external;
    /**
     * Creates a new IntentMatch instance.
     *
     * @param intentMatch - External reference to the native intent match implementation
     * @internal
     */
    constructor(intentMatch: ExternalReference);
    /**
     * Gets the name of the matched intent.
     *
     * @returns Name of the matched intent
     */
    getName(): string;
    /**
     * Gets the confidence score of the intent match.
     *
     * @returns Confidence score (typically between 0 and 1)
     */
    getScore(): number;
    /**
     * Returns the external reference to the native intent match implementation.
     *
     * @returns External reference object
     * @internal
     */
    getExternal(): ExternalReference;
    /**
     * Cleans up resources associated with this intent match.
     */
    destroy(): void;
}
