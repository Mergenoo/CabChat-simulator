import { ExternalReference } from '../../common/types';
import { EntityInterface } from './entity';
/**
 * Interface for configuring Named Entity Recognition creation.
 * @internal
 */
export interface NERCreationConfigInterface {
    /** Array of entity definitions to recognize */
    entities: EntityInterface[];
}
/**
 * Class for configuring Named Entity Recognition (NER) creation.
 * Manages settings for creating NER instances with specific entity definitions.
 * @internal
 */
export declare class NERCreationConfig {
    private external;
    private entitiesVector;
    /**
     * Creates a new NERCreationConfig instance.
     *
     * @param config - Configuration parameters for NER creation
     */
    constructor(config: NERCreationConfigInterface);
    /**
     * Returns the external reference to the native config implementation.
     *
     * @returns External reference object
     */
    getExternal(): ExternalReference;
    /**
     * Cleans up resources associated with this configuration.
     */
    destroy(): void;
}
