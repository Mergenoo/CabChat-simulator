import { TextGenerationConfigInterface } from '../../primitives/llm';
/**
 * Interface for configuring flash memory behavior.
 * Flash memory represents short-term, immediately accessible memories from the conversation.
 */
export interface FlashMemoryConfigInterface {
    /** Maximum size of dialogue to consider for memory creation */
    dialogueCutoffSize?: number;
    /** Number of conversation turns to maintain in history */
    nHistoryTurns?: number;
    /** Threshold for similarity when determining if memories are duplicates */
    memoriesSimilarityThreshold?: number;
    /** Maximum number of flash memories to maintain */
    maxFlashMemory?: number;
    /** Maximum number of topics to extract per memory */
    maxTopicsPerMemory?: number;
    /** Configuration for text generation used in memory processing */
    textGenerationConfig?: TextGenerationConfigInterface;
}
/**
 * Class for configuring flash memory settings.
 * Manages parameters that control how short-term memory is created and maintained.
 * @internal
 */
export declare class FlashMemoryConfig {
    private external;
    /**
     * Creates a new FlashMemoryConfig instance.
     *
     * @param params - Optional configuration parameters
     */
    constructor(params?: FlashMemoryConfigInterface);
    /**
     * Returns the external reference to the native config implementation.
     *
     * @returns External reference object
     */
    getExternal(): any;
    /**
     * Cleans up resources associated with this configuration.
     */
    destroy(): void;
}
