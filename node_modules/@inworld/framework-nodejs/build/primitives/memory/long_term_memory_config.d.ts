import { TextGenerationConfigInterface } from '../../primitives/llm';
/**
 * Interface for configuring long-term memory behavior.
 * Long-term memory represents persistent memories across multiple conversations.
 */
export interface LongTermMemoryConfigInterface {
    /** Maximum length of topic summary to append to long-term memory */
    maxTopicSummaryLenToAppend?: number;
    /** Maximum number of flash memories to consider for long-term memory creation */
    maxNumberOfFlashMemory?: number;
    /** Maximum number of topics to maintain in long-term memory */
    maxNumberOfTopics?: number;
    /** Configuration for text generation used in memory processing */
    textGenerationConfig?: TextGenerationConfigInterface;
}
/**
 * Class for configuring long-term memory settings.
 * Manages parameters that control how persistent memories are created and maintained.
 * @internal
 */
export declare class LongTermMemoryConfig {
    private external;
    /**
     * Creates a new LongTermMemoryConfig instance.
     *
     * @param params - Optional configuration parameters
     */
    constructor(params?: LongTermMemoryConfigInterface);
    /**
     * Returns the external reference to the native config implementation.
     *
     * @returns External reference object
     */
    getExternal(): any;
    /**
     * Cleans up resources associated with this configuration.
     */
    destroy(): void;
}
