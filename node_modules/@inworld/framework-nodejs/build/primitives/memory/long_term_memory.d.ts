import { KnowledgeCollection } from '../knowledge/knowledge_collection';
/**
 * Interface for long-term memory initialization.
 */
export interface LongTermMemoryInterface {
    /** Optional knowledge collection to use as initial memory */
    knowledgeCollection?: KnowledgeCollection;
}
/**
 * Class representing long-term memory.
 * Manages persistent memories that are maintained across multiple conversations.
 * @internal
 */
export declare class LongTermMemory {
    private external;
    /**
     * Creates a new LongTermMemory instance.
     *
     * @param params - Optional initialization parameters
     */
    constructor(params?: LongTermMemoryInterface);
    /**
     * Returns the external reference to the native long-term memory implementation.
     *
     * @returns External reference object
     */
    getExternal(): any;
    /**
     * Cleans up resources associated with this long-term memory.
     */
    destroy(): void;
}
