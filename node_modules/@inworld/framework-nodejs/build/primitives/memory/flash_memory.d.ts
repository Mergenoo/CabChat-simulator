import { KnowledgeCollection } from '../knowledge/knowledge_collection';
/**
 * Interface for flash memory initialization.
 */
export interface FlashMemoryInterface {
    /** Optional knowledge collection to use as initial memory */
    knowledgeCollection?: KnowledgeCollection;
}
/**
 * Class representing flash memory (short-term memory).
 * Manages recent conversation memories that are immediately accessible to the agent.
 * @internal
 */
export declare class FlashMemory {
    private external;
    /**
     * Creates a new FlashMemory instance.
     *
     * @param params - Optional initialization parameters
     */
    constructor(params?: FlashMemoryInterface);
    /**
     * Returns the external reference to the native flash memory implementation.
     *
     * @returns External reference object
     */
    getExternal(): any;
    /**
     * Cleans up resources associated with this flash memory.
     */
    destroy(): void;
}
