import { MapStringInterface } from '../../common';
/**
 * Represents a message in a chat system.
 */
export interface LLMMessageInterface extends MapStringInterface {
    /** The role of the message: user, assistant, system, etc. */
    role: string;
    /** The content of the message */
    content: string;
}
/**
 * Class for managing and processing messages.
 * @internal
 */
export declare class Messages {
    private mapString;
    /**
     * Creates a new Messages instance.
     * @param messages - Array of LLMMessageInterface objects.
     */
    constructor(messages: LLMMessageInterface[]);
    /**
     * Gets the external representation of the messages.
     * @returns The external representation of the messages.
     */
    getExternal(): any;
    /**
     * Destroys the Messages instance.
     */
    destroy(): void;
}
