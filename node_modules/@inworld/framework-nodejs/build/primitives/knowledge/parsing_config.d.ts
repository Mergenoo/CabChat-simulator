import { ExternalReference } from '../../common/types';
import { ParsingConfig as ParsingConfigType } from './types';
/**
 * Class for parsing documents into chunks
 * @internal
 */
export declare class ParsingConfig {
    private external;
    /**
     * Creates a new ParsingConfig instance
     * @param config Configuration parameters
     */
    constructor(config: ParsingConfigType);
    /**
     * Gets the reference to the native object
     * @returns Native object reference
     */
    getExternal(): ExternalReference;
    /**
     * Destroys the native object
     */
    destroy(): void;
}
