import { RetrievalConfigInterface } from '../../common/types';
/**
 * Class for configuring knowledge retrieval settings.
 * Controls how knowledge is retrieved and filtered from the knowledge base.
 * @internal
 */
export declare class RetrievalConfig {
    private external;
    /**
     * Creates a new RetrievalConfig instance.
     *
     * @param config - Configuration parameters for knowledge retrieval
     */
    constructor(config: RetrievalConfigInterface);
    /**
     * Returns the external reference to the native config implementation.
     *
     * @returns External reference object
     */
    getExternal(): any;
    /**
     * Cleans up resources associated with this configuration.
     */
    destroy(): void;
}
