import { ExternalReference } from '../../common/types';
import { KnowledgeCompileConfig as KnowledgeCompileConfigType } from './types';
/**
 * Class for compiling knowledge from files or records
 * @internal
 */
export declare class KnowledgeCompileConfig {
    private external;
    private parsingConfig;
    /**
     * Creates a new KnowledgeCompileConfig instance
     * @param config Configuration parameters
     */
    constructor(config: KnowledgeCompileConfigType);
    /**
     * Gets the reference to the native object
     * @returns Native object reference
     */
    getExternal(): ExternalReference;
    /**
     * Destroys the native object
     */
    destroy(): void;
}
