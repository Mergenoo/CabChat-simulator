import { ExternalReference } from '../../common/types';
import { LocalKnowledgeConfigInterface } from './types';
/**
 * Class for local knowledge system configuration
 * Configuration for local knowledge system
 * @internal
 */
export declare class LocalKnowledgeConfig {
    private external;
    private knowledgeGetConfig;
    private knowledgeCompileConfig;
    /**
     * Creates a new LocalKnowledgeConfig instance
     * @param config Configuration parameters
     */
    constructor(config: LocalKnowledgeConfigInterface);
    /**
     * Gets the reference to the native object
     * @returns Native object reference
     */
    getExternal(): ExternalReference;
    /**
     * Destroys the native object
     */
    destroy(): void;
}
