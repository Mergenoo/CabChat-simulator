import { TextEmbedder } from '../embedder/embedder';
import { KnowledgeCollection } from './knowledge_collection';
/**
 * Interface for parameters needed to create a knowledge collection.
 */
export interface CreateKnowledgeCollectionInterface {
    /** Text embedder for generating embeddings */
    embedder: TextEmbedder;
    /** Array of knowledge text entries */
    knowledge: string[];
}
/**
 * Factory class for creating KnowledgeCollection instances.
 * Provides functionality to create and initialize knowledge collections.
 */
export declare class KnowledgeCollectionFactory {
    /**
     * Creates a new KnowledgeCollection with the specified embedder and knowledge.
     *
     * @param params - Parameters for knowledge collection creation
     * @returns Promise resolving to a new KnowledgeCollection instance
     * @throws Error if creation fails
     */
    static create(params: CreateKnowledgeCollectionInterface): Promise<KnowledgeCollection>;
}
