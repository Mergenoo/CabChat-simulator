import { ExternalReference } from '../../common/types';
import { KnowledgeGetConfig as KnowledgeGetConfigType } from './types';
/**
 * Class for knowledge retrieval configuration
 * @internal
 */
export declare class KnowledgeGetConfig {
    private external;
    private retrievalConfig;
    /**
     * Creates a new KnowledgeGetConfig instance
     * @param config Configuration parameters
     */
    constructor(config: KnowledgeGetConfigType);
    /**
     * Gets the reference to the native object
     * @returns Native object reference
     */
    getExternal(): ExternalReference;
    /**
     * Destroys the native object
     */
    destroy(): void;
}
